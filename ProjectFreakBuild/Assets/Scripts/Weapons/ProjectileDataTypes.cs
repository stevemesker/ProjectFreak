namespace ProjectileDataTypes
{
    public enum ProjectilePattern
    {
        single,
        line,
        cone,
        ring,
        charge
    }

    public enum ProjectileMoveType
    {
        normal,
        hitscan,
        trail
    }
}
